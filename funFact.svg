<svg fill="none" viewBox="0 0 600 70" xmlns="http://www.w3.org/2000/svg">
  <style>
    .colorful-text span {
      display: inline-block;
    }
  </style>
  <text x="0" y="50" fill="#000">
    - ⚡ Fun fact <tspan style="font-weight:bold;">I think I am </tspan>
    <tspan class="colorful-text">creative with colours</tspan>
  </text>
  <script>
    function getRandomColor() {
      var letters = '0123456789ABCDEF';
      var color = '#';
      for (var i = 0; i < 6; i++) {
        color += letters[Math.floor(Math.random() * 16)];
      }
      return color;
    }
    var textElement = document.querySelector('.colorful-text');
    var words = textElement.textContent.split(' ');
    textElement.textContent = '';

    words.forEach(function (word) {
      var tspan = document.createElementNS('http://www.w3.org/2000/svg', 'tspan');
      tspan.textContent = word;
      tspan.style.fill = getRandomColor();
      textElement.appendChild(tspan);
      textElement.appendChild(document.createTextNode(' '));
    });
  </script>
</svg>
